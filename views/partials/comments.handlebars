<section>

    <div class="row">
        {{#each this}}
        <div class="col-md-8 bg-dark">
            <p class="text-white pt-2 text-right">{{this.text}}</p>
        </div>
        {{/each}}
    </div>
</section>